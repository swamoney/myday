Database setup files for MyDay
For disaster recovery: run myday_setup.sql — it rebuilds the ENTIRE database in one go.
All other files here are historical steps, already included in it. Keep them; never need them.
